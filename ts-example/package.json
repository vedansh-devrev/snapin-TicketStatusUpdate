{
    "name": "flow-lambda-poc",
    "version": "1.0.0",
    "private": true,
    "devDependencies": {
        "@types/aws-lambda": "8.10.108",
        "@types/node-fetch": "^2.6.2",
        "esbuild": "0.15.13"
    },
    "scripts": {
        "prebuild": "rm -rf dist",
        "build": "esbuild main.ts --bundle --minify --sourcemap --platform=node --target=es2020 --outfile=dist/index.js",
        "postbuild": "cd dist && zip -r index.zip index.js*"
    },
    "dependencies": {
        "node-fetch": "^2.6.9"
    }
}
